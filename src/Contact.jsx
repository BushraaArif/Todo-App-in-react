
import React from 'react';

const Contact = (props) => {
    return (
        <div>
            <h1>Contact Page</h1>
            <p>{props.description}</p>
        </div>
    );
};

export default Contact;
