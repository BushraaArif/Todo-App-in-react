
import React from 'react';

const About = (props) => {
    return (
        <div>
            <h1>About Page</h1>
            <p>{props.description}</p>
        </div>
    );
};

export default About;
